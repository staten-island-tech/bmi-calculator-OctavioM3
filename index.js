function getBMI (x,y){
    return 20;
}
//conditional statement if/eles if/else

function check (x,y){
    if(x <= "30"){
        console.log("healthy");
    } else{
        console.log("that is false")
    }
}
//check("horsey");

function init(){
    y = weight;
    z = height;
    let x = getBMI(y,z);
    check(x);
}